
<ol class="breadcrumb page-breadcrumb">
    <li class="breadcrumb-item">Financeiro</li>
    <li class="breadcrumb-item active">Despesa Mensal Processamento</li>
</ol>

<div class="subheader">
    <h1 class="subheader-title">
        <i class=""></i> Processamento de Despesas Mensais
    </h1>
</div>

<div id="knockoutPesquisaDespesaMensalProcessamento">
    <div class="panel">
        <div class="panel-container">
            <div class="panel-content">
                <button class="btn btn-primary waves-effect waves-themed" data-bind="click: ExibirFiltros.eventClick, id: ExibirFiltros.id">
                    <i class="fal fa-search"></i>
                    Pesquisar Processamentos de Despesas Mensais
                </button>

                <div class="mt-2" data-bind="id: ExibirFiltros.idFade, fadeVisible: ExibirFiltros.visibleFade">
                    <form>
                        <div class="row">
                            <div class="col-12 col-md-9" data-bind="visible: TipoDespesa.visible">
                                <div class="form-group">
                                    <label class="form-label" data-bind="text: TipoDespesa.text, attr: { for: TipoDespesa.id }"></label>
                                    <div class="input-group">
                                        <input type="text" class="form-control" data-bind="css: TipoDespesa.requiredClass, value: TipoDespesa.val, valueUpdate: 'afterkeydown', attr: { maxlength: TipoDespesa.maxlength, id : TipoDespesa.id}" />
                                        <div class="input-group-append">
                                            <button class="btn btn-primary waves-effect waves-themed" data-bind="attr: { id: TipoDespesa.idBtnSearch}" type="button" aria-label="Buscar">
                                                <i class="fal fa-search"></i>
                                            </button>
                                        </div>
                                    </div>
                                </div>
                            </div>
                            <div class="col-12 col-md-3" data-bind="visible: Mes.visible">
                                <div class="form-group">
                                    <label class="form-label" data-bind="text: Mes.text, attr: { for: Mes.id }"></label>
                                    <select class="form-control" data-bind="options: Mes.options, optionsText: 'text', optionsValue: 'value', value: Mes.val, attr: { id : Mes.id}"></select>
                                </div>
                            </div>
                        </div>
                    </form>
                    <div class="d-flex align-items-center justify-content-end mb-3">
                        <button class="btn btn-primary waves-effect waves-themed btnPesquisarFiltroPesquisa" data-bind="click: Pesquisar.eventClick, attr: { id: Pesquisar.id}" type="button">
                            <i class="fal fa-search"></i><span data-bind="text: Pesquisar.text"></span>
                        </button>
                    </div>
                    <table width="100%" class="table table-bordered table-hover" data-bind="attr: { id: Pesquisar.idGrid}" cellspacing="0"></table>
                </div>
            </div>
        </div>
    </div>
</div>

<div class="panel">
    <div class="panel-container">
        <div class="panel-hdr">
            <h3>
                <i class="fal fa-edit"></i>
                Selecione os filtros desejados para a geração das despesas mensais
            </h3>
        </div>
        <div class="panel-content">
            <form>
                <div class="row" id="knockoutCadastroDespesaMensalProcessamento">
                    <div class="col-12 col-md-6" data-bind="visible: TipoDespesa.visible">
                        <div class="form-group">
                            <label class="form-label" data-bind="text: TipoDespesa.text, attr: { for: TipoDespesa.id }"></label>
                            <div class="input-group">
                                <input type="text" class="form-control" data-bind="css: TipoDespesa.requiredClass, value: TipoDespesa.val, valueUpdate: 'afterkeydown', attr: { maxlength: TipoDespesa.maxlength, id : TipoDespesa.id}" />
                                <div class="input-group-append">
                                    <button class="btn btn-primary waves-effect waves-themed" data-bind="attr: { id: TipoDespesa.idBtnSearch}" type="button" aria-label="Buscar">
                                        <i class="fal fa-search"></i>
                                    </button>
                                </div>
                            </div>
                        </div>
                    </div>
                    <div class="col-12 col-md-2" data-bind="visible: Mes.visible">
                        <div class="form-group">
                            <label class="form-label" data-bind="text: Mes.text, attr: { for: Mes.id }"></label>
                            <select class="form-control" data-bind="options: Mes.options, enable: Mes.enable, optionsText: 'text', optionsValue: 'value', value: Mes.val, attr: { id : Mes.id}"></select>
                        </div>
                    </div>
                    <div class="col-12 col-md-2" data-bind="visible: DataPagamento.visible">
                        <div class="form-group">
                            <label class="form-label" data-bind="text: DataPagamento.text, attr: { for: DataPagamento.id }"></label>
                            <input type="text" class="form-control" data-bind="value: DataPagamento.val, enable: DataPagamento.enable, valueUpdate: 'afterkeydown', attr: { maxlength: DataPagamento.maxlength, id : DataPagamento.id}" />
                        </div>
                    </div>
                    <div class="col-12 col-md-2 mt-5">
                        <button class="btn btn-success" type="button" data-bind="click: BuscarDespesa.eventClick, visible: BuscarDespesa.visible, enable: BuscarDespesa.enable, attr: { value: BuscarDespesa.text, id : BuscarDespesa.id}">
                            <i class="fal fa-search"></i><span data-bind="text: BuscarDespesa.text"></span>
                        </button>
                    </div>
                    <div class="row">
                        <div class="col-12 d-flex justify-content-end">
                            <button type="button" class="btn btn-labeled btn-default mb-2" data-bind="click: SelecionarTodos.eventClick, attr: { id: SelecionarTodos.id}, visible: SelecionarTodos.visible">
                                <i class="fal fa-check"></i>
                                <span data-bind="text: SelecionarTodos.text"></span>
                            </button>
                        </div>
                        <table width="100%" class="table table-bordered table-hover" data-bind="enable: Despesas.enable, attr: { id: Despesas.idGrid}" cellspacing="0"></table>
                    </div>
                    <div class="row mt-2">
                        <div class="col-6">
                            <section data-bind="visible: ValorTotalPagar.visible" class="col-12 col-md-12 mb-3">
                                <label><b data-bind="text: ValorTotalPagar.text"></b> <span data-bind="text: ValorTotalPagar.val"></span></label>
                            </section>
                        </div>
                        <div class="col-6 d-flex justify-content-end">
                            <button type="button" class="btn btn-labeled btn-success" data-bind="click: AdicionarNovaDespesa.eventClick, visible: AdicionarNovaDespesa.visible, enable: AdicionarNovaDespesa.enable, attr: { value: AdicionarNovaDespesa.text, id : AdicionarNovaDespesa.id}">
                                <span data-bind="text: AdicionarNovaDespesa.text"></span>
                            </button>
                        </div>
                    </div>
                    <div class="row">
                        <div class="col-6">
                            <section data-bind="visible: QuantidadeDespesas.visible" class="col-12 col-md-12">
                                <label><b data-bind="text: QuantidadeDespesas.text"></b> <span data-bind="text: QuantidadeDespesas.val"></span></label>
                            </section>
                        </div>
                    </div>
                </div>
                <hr />
                <footer id="knockoutCRUDDespesaMensalProcessamento">
                    <div class="row mt-2">
                        <div class="col-12 d-flex justify-content-end">
                            <button type="button" class="btn btn-labeled btn-default mx-2" data-bind="click: Limpar.eventClick,visible: Limpar.visible, attr: { value: Limpar.text, id : Limpar.id}">
                                <span data-bind="text: Limpar.text"></span>
                            </button>
                            <button type="button" class="btn btn-labeled btn-success" data-bind="click: Processar.eventClick,visible: Processar.visible, attr: { value: Processar.text, id : Processar.id}">
                                <span data-bind="text: Processar.text"></span>
                            </button>
                        </div>
                    </div>
                </footer>
            </form>
        </div>
    </div>
</div>

<div class="modal fade" id="divModalDespesaMensal" tabindex="-1" role="dialog" aria-labelledby="exampleModalLabel" aria-hidden="true">
    <div class="modal-dialog modal-xl modal-dialog-centered" role="document">
        <div class="modal-content">
            <div class="modal-header">
                <h4 class="modal-title">Cadastro de Despesa Mensal</h4>
                <button type="button" class="btn-close" data-bs-dismiss="modal">
                    <i class="fal fa-times"></i>
                </button>
            </div>
            <div class="modal-body">
                <div id="conteudoDespesaMensal">

                </div>
            </div>
            <div class="modal-footer">
            </div>
        </div>
    </div>
</div>

<script src="~/scripts/despesaMensalProcessamento"></script>
<script src="~/scripts/despesaMensal"></script>
<script type="text/javascript">
    pageSetUp();

    $(document).ready(function () {
        loadDespesaMensalProcessamento();
    });

</script>

