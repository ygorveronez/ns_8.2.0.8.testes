
<ol class="breadcrumb page-breadcrumb">
    <li class="breadcrumb-item">Financeiros</li>
    <li class="breadcrumb-item active">Fechamento Diário</li>
</ol>
<div class="subheader">
    <h1 class="subheader-title">
        <i></i> Fechamento Diário
    </h1>
</div>

<div id="knockoutPesquisaFechamentoDiario">
    <div class="panel">
        <div class="panel-container">
            <div class="panel-content">
                <button class="btn btn-primary waves-effect waves-themed" data-bind="click: ExibirFiltros.eventClick, id: ExibirFiltros.id">
                    <i class="fal fa-search"></i>
                    Pesquisar Fechamento Diário
                </button>

                <div class="mt-3" data-bind="id: ExibirFiltros.idFade, fadeVisible: ExibirFiltros.visibleFade">
                    <form>
                        <div class="row">
                            <div class="col-12 col-md-3" data-bind="visible: DataFechamentoInicial.visible">
                                <div class="form-group">
                                    <label class="form-label" data-bind="text: DataFechamentoInicial.text, attr: { for: DataFechamentoInicial.id }"></label>
                                    <input type="text" class="form-control" data-bind="value: DataFechamentoInicial.val, valueUpdate: 'afterkeydown', attr: { maxlength: DataFechamentoInicial.maxlength, id : DataFechamentoInicial.id}" />
                                </div>
                            </div>
                            <div class="col-12 col-md-3" data-bind="visible: DataFechamentoFinal.visible">
                                <div class="form-group">
                                    <label class="form-label" data-bind="text: DataFechamentoFinal.text, attr: { for: DataFechamentoFinal.id }"></label>
                                    <input type="text" class="form-control" data-bind="value: DataFechamentoFinal.val, valueUpdate: 'afterkeydown', attr: { maxlength: DataFechamentoFinal.maxlength, id : DataFechamentoFinal.id}" />
                                </div>
                            </div>
                            <div class="col-12 col-md-6" data-bind="visible: Empresa.visible">
                                <div class="form-group">
                                    <label class="form-label" data-bind="text: Empresa.text, attr: { for: Empresa.id }"></label>
                                    <div class="input-group">
                                        <input type="text" data-bind="css: Empresa.requiredClass, value: Empresa.val, valueUpdate: 'afterkeydown', attr: { 'aria-label': Empresa.text, maxlength: Empresa.maxlength, id: Empresa.id, name: Empresa.id }" />
                                        <div class="input-group-append">
                                            <button class="btn btn-primary waves-effect waves-themed is-invalid" type="button" data-bind="attr: { id: Empresa.idBtnSearch}">
                                                <i class="fal fa-search"></i>
                                            </button>
                                        </div>
                                    </div>
                                </div>
                            </div>

                        </div>
                    </form>
                    <div class="d-flex align-items-center justify-content-end mb-3">
                        <button class="btn btn-primary waves-effect waves-themed btnPesquisarFiltroPesquisa" data-bind="click: Pesquisar.eventClick, attr: { id: Pesquisar.id}" type="button">
                            <i class="fal fa-search"></i><span data-bind="text: Pesquisar.text"></span>
                        </button>
                    </div>
                    <table width="100%" class="table table-bordered table-hover" data-bind="attr: { id: Pesquisar.idGrid}" cellspacing="0"></table>
                </div>
            </div>
        </div>
    </div>
</div>

<div class="panel">
    <div class="panel-container">
        <div class="panel-hdr">
            <h3>
                <i class="fal fa-edit"></i>
                Cadastrar Fechamento Diário
            </h3>
        </div>
        <div class="panel-content">
            <form id="knockoutCadastroFechamentoDiario">
                <div class="row">
                    <div class="col-12 col-md-3" data-bind="visible: DataFechamento.visible">
                        <div class="form-group">
                            <label class="form-label" data-bind="text: DataFechamento.text, attr: { for: DataFechamento.id }"></label>
                            <input type="text" class="form-control" data-bind="css: DataFechamento.requiredClass,value: DataFechamento.val, valueUpdate: 'afterkeydown', attr: { maxlength: DataFechamento.maxlength, id : DataFechamento.id}" />
                        </div>
                    </div>
                    <div class="col-12 col-md-6" data-bind="visible: Empresa.visible">
                        <div class="form-group">
                            <label class="form-label" data-bind="text: Empresa.text, attr: { for: Empresa.id }"></label>
                            <div class="input-group">
                                <input type="text" data-bind="css: Empresa.requiredClass, value: Empresa.val, valueUpdate: 'afterkeydown', attr: { 'aria-label': Empresa.text, maxlength: Empresa.maxlength, id: Empresa.id, name: Empresa.id }" />
                                <div class="input-group-append">
                                    <button class="btn btn-primary waves-effect waves-themed is-invalid" type="button" data-bind="attr: { id: Empresa.idBtnSearch}">
                                        <i class="fal fa-search"></i>
                                    </button>
                                </div>
                            </div>
                        </div>
                    </div>
                    <div class="col-12 col-md-12" data-bind="visible: BloquearApenasDocumentoEntrada.visible">
                        <div class="form-group">
                            <div class="custom-control custom-checkbox">
                                <input type="checkbox" class="custom-control-input" data-bind="checked: BloquearApenasDocumentoEntrada.val, attr: { maxlength: BloquearApenasDocumentoEntrada.maxlength, name: BloquearApenasDocumentoEntrada.id, id: BloquearApenasDocumentoEntrada.id }">
                                <label class="custom-control-label" data-bind="text: BloquearApenasDocumentoEntrada.text, attr: { for: BloquearApenasDocumentoEntrada.id }"></label>
                            </div>
                        </div>
                    </div>
                </div>
                <footer>
                    <div class="row">
                        <div class="col-6">
                            <button type="button" class="btn btn-labeled btn-default" data-bind="click: Cancelar.eventClick, visible: Cancelar.visible, attr: { id : Cancelar.id}">
                                <i class="fal fa-ban"></i>
                                <span data-bind="text: Cancelar.text"></span>
                            </button>
                        </div>
                        <div class="col-6 d-flex justify-content-end">

                            <button type="button" class="btn btn-labeled btn-success" data-loading-text="Aguarde..." data-bind="click: Gerar.eventClick, visible: Gerar.visible, attr: { id : Gerar.id}">
                                <span data-bind="text: Gerar.text"></span>
                            </button>
                        </div>
                    </div>
                </footer>
            </form>
        </div>
    </div>
</div>

<div id="divModalDocumentosPendentes" class="modal fade" role="dialog" aria-hidden="true">
    <div id="knockoutDocumentosPendentes" class="modal-dialog modal-lg modal-dialog-centered">
        <div class="modal-content">
            <div class="modal-header">
                <h4 class="modal-title">Atenção: Não foi possível realizar o fechamento diário!</h4>
                <button type="button" class="btn-close" data-bs-dismiss="modal">
                    <i class="fal fa-times"></i>
                </button>
            </div>
            <div class="modal-body">
                <div class="row" data-bind="visible: Descricao.visible">
                    <div class="col-12 col-md-12 mb-12" data-bind="attr: {title: Descricao.val}">
                        <strong data-bind="text: Descricao.text"></strong><span data-bind="text: Descricao.val"></span>
                    </div>
                </div>
                <div class="d-flex justify-content-end mt-3">
                    <button class="btn btn-default me-2" type="button" data-bind="click: Fechar.eventClick, attr: { id : Fechar.id}">
                        <i class="fal fa-times"></i><span data-bind="text: Fechar.text"></span>
                    </button>
                </div>
            </div>
        </div>
    </div>
</div>

<script src="~/scripts/fechamentoDiario"></script>
<script type="text/javascript">
    pageSetUp();

    $(document).ready(function () {
        LoadFechamentoDiario();
    });
</script>