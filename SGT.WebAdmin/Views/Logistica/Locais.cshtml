

<style>
    .button-outline {
        margin-top: 8px;
        padding: 8px;
        border: solid 1px;
        border-color: #02243D;
        border-radius: 3px;
        background: none;
        font-weight: 700;
        color: #02243D;
    }

        .button-outline > i {
            font-weight: 500;
            color: #02243D;
            margin: 2px;
        }
</style>

<ol class="breadcrumb page-breadcrumb">
    <li class="breadcrumb-item">Logística</li>
    <li class="breadcrumb-item active">Locais</li>
</ol>
<div class="subheader">
    <h1 class="subheader-title">
        Locais
    </h1>
</div>

<div id="knockoutPesquisaLocais">
    <div class="panel">
        <div class="panel-container">
            <div class="panel-content">
                <button class="btn btn-primary waves-effect waves-themed" data-bind="click: ExibirFiltros.eventClick, id: ExibirFiltros.id">
                    <i class="fal fa-search"></i>
                    Pesquisar Locais
                </button>
                <div class="mt-3" data-bind="id: ExibirFiltros.idFade, fadeVisible: ExibirFiltros.visibleFade">
                    <form>
                        <div class="row">

                            <div class="col-12 col-md-12" data-bind="visible: Descricao.visible">
                                <div class="form-group">
                                    <label class="form-label" data-bind="text: Descricao.text, attr: { for: Descricao.id }"></label>
                                    <input type="text" class="form-control" data-bind="value: Descricao.val, valueUpdate: 'afterkeydown', attr: { maxlength: Descricao.maxlength, id : Descricao.id}" />
                                </div>
                            </div>
                        </div>
                    </form>
                    <div class="d-flex align-items-center justify-content-end mb-3">
                        <button class="btn btn-primary waves-effect waves-themed btnPesquisarFiltroPesquisa" data-bind="click: Pesquisar.eventClick, attr: { id: Pesquisar.id}" type="button">
                            <i class="fal fa-search"></i><span data-bind="text: Pesquisar.text"></span>
                        </button>
                    </div>
                    <div class="widget-body">
                        <table width="100%" class="table table-bordered table-hover" data-bind="attr: { id: Pesquisar.idGrid}" cellspacing="0"></table>
                    </div>
                </div>
            </div>
        </div>
    </div>
</div>
<div class="panel" id="wid-id-4" data-widget-colorbutton="false" data-widget-deletebutton="false" data-widget-editbutton="false" data-widget-custombutton="false">
    <div class="panel-container">
        <div class="panel-content">
            <ul class="nav nav-tabs">
                <li class="nav-item active" id="litabAreaRisco">
                    <a href="#tabAreaRisco" class="nav-link active" data-bs-toggle="tab">
                        <i class="fal fa-edit"></i> <span class="hidden-mobile hidden-tablet">Locais</span>
                    </a>
                </li>
                <li class="nav-item" id="litTabGeoLocalizacao">
                    <a href="#tabGeoLocalizacao" onclick="abaLocaisGeoLocalizacaoClick()" class="nav-link" data-bs-toggle="tab">
                        <i class="fal fa-map-marker-alt"></i><span class="hidden-mobile hidden-tablet">Geolocalização</span>
                    </a>
                </li>
            </ul>
            <div class="tab-content border border-top-0 p-3" id="myTabContent">
                <div class="tab-pane fade active show" id="tabAreaRisco" role="tabpanel">
                    <form novalidate id="knockoutLocais">
                        <div class="row">

                            <div class="col-12 col-md-4" data-bind="visible: Descricao.visible">
                                <div class="form-group">
                                    <label class="form-label" data-bind="text: Descricao.text, attr: { for: Descricao.id }"></label>
                                    <input type="text" class="form-control" data-bind="css: Descricao.requiredClass, value: Descricao.val, valueUpdate: 'afterkeydown', attr: { maxlength: Descricao.maxlength, id : Descricao.id}" />
                                </div>
                            </div>

                            <div class="col-12 col-md-6 col-lg-2" data-bind="visible: TipoLocal.visible">
                                <div class="form-group">
                                    <label class="form-label" data-bind="text: TipoLocal.text, attr: { for: TipoLocal.id }"></label>
                                    <select class="form-control" data-bind="options: TipoLocal.options, optionsText: 'text', optionsValue: 'value', value: TipoLocal.val, attr: { id : TipoLocal.id}"></select>
                                </div>
                            </div>

                            <div class="col-12 col-md-6 col-lg-2" data-bind="visible: TipoArea.visible">
                                <div class="form-group">
                                    <label class="form-label" data-bind="text: TipoArea.text, attr: { for: TipoArea.id }"></label>
                                    <select class="form-control" data-bind="options: TipoArea.options, optionsText: 'text', optionsValue: 'value', value: TipoArea.val, attr: { id : TipoArea.id}"></select>
                                </div>
                            </div>

                            <div class="col-12 col-md-4" data-bind="visible: Filial.visible">
                                <div class="form-group">
                                    <label class="form-label" data-bind="text: Filial.text, attr: { for: Filial.id }"></label>
                                    <div class="input-group">
                                        <input type="text" data-bind="css: Filial.requiredClass, value: Filial.val, valueUpdate: 'afterkeydown', attr: { 'aria-label': Filial.text, maxlength: Filial.maxlength, id: Filial.id, name: Filial.id }" />
                                        <div class="input-group-append">
                                            <button class="btn btn-primary waves-effect waves-themed" type="button" data-bind="attr: { id: Filial.idBtnSearch}">
                                                <i class="fal fa-search"></i>
                                            </button>
                                        </div>
                                    </div>
                                </div>
                            </div>
                            <div class="col-12 col-md-12" data-bind="visible: Observacao.visible">
                                <div class="form-group">
                                    <label class="form-label" data-bind="text: Observacao.text, attr: { for: Observacao.id }"></label>
                                    <textarea class="form-control" rows="5" type="text" data-bind="value: Observacao.val, valueUpdate: 'afterkeydown', attr: { maxlength: Observacao.maxlength, id : Observacao.id}"></textarea>
                                </div>
                            </div>
                        </div>
                    </form>
                    <div id="divCadastroRaiosProximidade">

                    </div>

                </div>

                <div class="tab-pane fade col-12 col-md-12" id="tabGeoLocalizacao" role="tabpanel">
                    <div class="mb-3" id="map" style="width: 100%; height: 650px; position: relative; overflow: hidden;"></div>
                </div>

                <div id="knockoutCRUDLocais">
                    <div data-bind="visible: AdicionarNovoRaio.visible">
                        <button type="button" class="button-outline" data-bind="click: AdicionarNovoRaio.eventClick, visible: AdicionarNovoRaio.visible, attr: { id : AdicionarNovoRaio.id}">
                            <i class="fal fa-plus"></i> <span data-bind="text: AdicionarNovoRaio.text"></span>
                        </button>
                    </div>
                    <footer class="d-flex justify-content-end">
                        <div class="ms-2" data-bind='component: {name: "import-file", params: { ko: Importar }}'></div>
                        <button type="button" class="btn btn-labeled btn-default ms-1" data-bind="click: Cancelar.eventClick, visible: Cancelar.visible, text: Cancelar.text, attr: { id : Cancelar.id}">
                        </button>
                        <button type="button" class="btn btn-danger ms-1" data-bind="click: Excluir.eventClick, visible: Excluir.visible, text: Excluir.text, attr: { id : Excluir.id}">
                        </button>
                        <button type="button" class="btn btn-success ms-1" data-bind="click: Atualizar.eventClick, visible: Atualizar.visible, text: Atualizar.text, attr: { id : Atualizar.id}">
                        </button>
                        <button type="button" class="btn btn-success ms-1" data-loading-text="Aguarde..." data-bind="click: Adicionar.eventClick, visible: Adicionar.visible, text: Adicionar.text, attr: { id : Adicionar.id}">
                        </button>
                    </footer>
                </div>
            </div>
        </div>
    </div>
</div>


<script src="~/scripts/locais"></script>
<script type="text/javascript">
    pageSetUp();

    $(document).ready(function () {
        loadLocais();
    });

</script>
