
<ol class="breadcrumb page-breadcrumb">
    <li class="breadcrumb-item">Notas Fiscais</li>
    <li class="breadcrumb-item">Nota Fiscal Observacao Carta Correção</li>
</ol>
<div class="subheader">
    <h1 class="subheader-title">
        <i></i>Observação Carta de Correção de Nota Fiscal
    </h1>
</div>
<div id="knockoutPesquisaNotaFiscalObservacaoCartaCorrecao">
    <div class="panel">
        <div class="panel-container">
            <div class="panel-content">
                <button class="btn btn-primary waves-effect waves-themed" data-bind="click: ExibirFiltros.eventClick, id: ExibirFiltros.id">
                    <i class="fal fa-search"></i>
                    Pesquisar Observação Carta de Correção de Nota Fiscal
                </button>

                <div class="mt-3" data-bind="id: ExibirFiltros.idFade, fadeVisible: ExibirFiltros.visibleFade">
                    <form>
                        <div class="row">
                            <div class="col-12 col-md-12" data-bind="visible : Especificacao.visible">
                                <div class="form-group">
                                    <label class="form-label" data-bind="text: Especificacao.text"></label>
                                    <input type="text" class="form-control" data-bind="value: Especificacao.val, attr: { maxlength: Especificacao.maxlength, id : Especificacao.id} , valueUpdate: 'afterkeydown'" />
                                </div>
                            </div>
                        </div>
                    </form>
                    <div class="d-flex align-items-center justify-content-end mb-3">
                        <button class="btn btn-primary waves-effect waves-themed btnPesquisarFiltroPesquisa" data-bind="click: Pesquisar.eventClick, attr: { id: Pesquisar.id}" type="button">
                            <i class="fal fa-search"></i><span data-bind="text: Pesquisar.text"></span>
                        </button>
                    </div>
                    <table width="100%" class="table table-bordered table-hover" data-bind="attr: { id: Pesquisar.idGrid}" cellspacing="0"></table>
                </div>
            </div>
        </div>
    </div>
</div>
<div class="panel">
    <div class="panel-container">
        <div class="panel-hdr">
            <h3>
                <i class="fal fa-edit"></i>
                Cadastrar Observação Carta de Correção de Nota Fiscal
            </h3>
        </div>
        <div class="panel-content">
            <form id="knockoutCadastroNotaFiscalObservacaoCartaCorrecao">
                <div class="row">
                    <div class="col-12 col-md-9" data-bind="visible : Especificacao.visible">
                        <div class="form-group">
                            <label class="form-label" data-bind="text: Especificacao.text"></label>
                            <input type="text" class="form-control" data-bind="css: Mensagem.requiredClass,value: Especificacao.val, attr: { maxlength: Especificacao.maxlength, id : Especificacao.id} , valueUpdate: 'afterkeydown'" />
                        </div>
                    </div>

                    <div class="col-12 col-md-3" data-bind="visible: Status.visible">
                        <div class="form-group">
                            <label class="form-label" data-bind="text: Status.text, attr: { for: Status.id }"></label>
                            <select class="form-control" data-bind="options: Status.options, optionsText: 'text', optionsValue: 'value', value: Status.val, attr: { id : Status.id}"></select>
                        </div>
                    </div>
                    <div class="col-12 col-md-12" data-bind="visible: Mensagem.visible">
                        <div class="form-group">
                            <label class="form-label" data-bind="text: Mensagem.text"></label>
                            <textarea rows="5" class="form-control" data-bind="css:Mensagem.requiredClass, value: Mensagem.val, attr: { maxlength: Mensagem.maxlength, id : Mensagem.id}"></textarea>
                        </div>
                    </div>
                </div>
                <footer class="d-flex justify-content-end">
                    <button type="button" class="btn btn-labeled btn-default ms-1" data-bind="click: Cancelar.eventClick, visible: Cancelar.visible, attr: { id : Cancelar.id}">
                        <span data-bind="text: Cancelar.text"></span>
                    </button>
                    <button type="button" class="btn btn-labeled btn-danger ms-1" data-bind="click: Excluir.eventClick, visible: Excluir.visible, attr: { id : Excluir.id}">
                        <span data-bind="text: Excluir.text"></span>
                    </button>
                    <button type="button" class="btn btn-labeled btn-success ms-1" data-bind="click: Atualizar.eventClick, visible: Atualizar.visible, attr: { id : Atualizar.id}">
                        <span data-bind="text: Atualizar.text"></span>
                    </button>
                    <button type="button" class="btn btn-labeled btn-success ms-1" data-loading-text="Aguarde..." data-bind="click: Adicionar.eventClick, visible: Adicionar.visible, attr: { id : Adicionar.id}">
                        <span data-bind="text: Adicionar.text"></span>
                    </button>
                </footer>
            </form>
        </div>
    </div>
</div>
<script src="~/scripts/notaFiscalObservacaoCartaCorrecao"></script>
<script type="text/javascript">
    pageSetUp();

    $(document).ready(function () {
        loadNotaFiscalObservacaoCartaCorrecao();
    });

</script>

