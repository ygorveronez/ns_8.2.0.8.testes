
<ol class="breadcrumb page-breadcrumb">
    <li class="breadcrumb-item">Pessoas</li>
    <li class="breadcrumb-item active">Pessoa Campo Obrigatorio</li>
</ol>
<div class="subheader">
    <h1 class="subheader-title">
        Campos Obrigatórios para a Pessoa
    </h1>
</div>

<div id="knockoutPesquisaPessoaCampoObrigatorio">
    <div class="panel">
        <div class="panel-container">
            <div class="panel-content">
                <button class="btn btn-primary waves-effect waves-themed" data-bind="click: ExibirFiltros.eventClick, id: ExibirFiltros.id">
                    <i class="fal fa-search"></i>
                    Pesquisar Campos Obrigatórios para a Pessoa
                </button>

                <div class="mt-3" data-bind="id: ExibirFiltros.idFade, fadeVisible: ExibirFiltros.visibleFade">
                    <form>
                        <div class="row">
                            <div class="col-12 col-md-3" data-bind="visible: Cliente.visible">
                                <div class="form-group">
                                    <label class="form-label" data-bind="text: Cliente.text, attr: { for: Cliente.id }"></label>
                                    <select class="form-control" data-bind="class: Cliente.requiredClass, options: Cliente.options, optionsText: 'text', optionsValue: 'value', value: Cliente.val, attr: { maxlength: Cliente.maxlength, id : Cliente.id}"></select>
                                </div>
                            </div>
                            <div class="col-12 col-md-3" data-bind="visible: Fornecedor.visible">
                                <div class="form-group">
                                    <label class="form-label" data-bind="text: Fornecedor.text, attr: { for: Fornecedor.id }"></label>
                                    <select class="form-control" data-bind="class: Fornecedor.requiredClass, options: Fornecedor.options, optionsText: 'text', optionsValue: 'value', value: Fornecedor.val, attr: { maxlength: Fornecedor.maxlength, id : Fornecedor.id}"></select>
                                </div>
                            </div>
                            <div class="col-12 col-md-3 align-self-center" data-bind="visible: Terceiro.visible">
                                <div class="form-group">
                                    <label class="form-label" data-bind="text: Terceiro.text, attr: { for: Terceiro.id }"></label>
                                    <select class="form-control" data-bind="class: Terceiro.requiredClass, options: Terceiro.options, optionsText: 'text', optionsValue: 'value', value: Terceiro.val, attr: { maxlength: Terceiro.maxlength, id : Terceiro.id}"></select>
                                </div>
                            </div>
                            <div class="col-12 col-md-3" data-bind="visible: Situacao.visible">
                                <div class="form-group">
                                    <label class="form-label" data-bind="text: Situacao.text, attr: { for: Situacao.id }"></label>
                                    <select class="form-control" data-bind="class: Situacao.requiredClass, options: Situacao.options, optionsText: 'text', optionsValue: 'value', value: Situacao.val, attr: { maxlength: Situacao.maxlength, id : Situacao.id}"></select>
                                </div>
                            </div>
                        </div>
                    </form>
                    <div class="d-flex align-items-center justify-content-end mb-3">
                        <button class="btn btn-primary waves-effect waves-themed btnPesquisarFiltroPesquisa" data-bind="click: Pesquisar.eventClick, attr: { id: Pesquisar.id}" type="button">
                            <i class="fal fa-search"></i><span data-bind="text: Pesquisar.text"></span>
                        </button>
                    </div>
                    <table width="100%" class="table table-bordered table-hover" data-bind="attr: { id: Pesquisar.idGrid}" cellspacing="0"></table>
                </div>
            </div>
        </div>
    </div>
</div>

<div class="panel">
    <div class="panel-container">
        <div class="panel-hdr">
            <h3>
                <i class="fal fa-edit"></i>
                Cadastrar Campos Obrigatórios para a Pessoa
            </h3>
        </div>
        <div class="panel-content">
            <form>
                <div class="row" id="knockoutPessoaCampoObrigatorio">
                    <div class="col-12 col-md-3 align-self-center" data-bind="visible: Cliente.visible">
                        <div class="form-group">
                            <div class="custom-control custom-checkbox">
                                <input type="checkbox" class="custom-control-input" data-bind="checked: Cliente.val, attr: { maxlength: Cliente.maxlength, name: Cliente.id, id: Cliente.id }">
                                <label class="custom-control-label" data-bind="text: Cliente.text, attr: { for: Cliente.id }"></label>
                            </div>
                        </div>
                    </div>
                    <div class="col-12 col-md-3 align-self-center" data-bind="visible: Fornecedor.visible">
                        <div class="form-group">
                            <div class="custom-control custom-checkbox">
                                <input type="checkbox" class="custom-control-input" data-bind="checked: Fornecedor.val, attr: { maxlength: Fornecedor.maxlength, name: Fornecedor.id, id: Fornecedor.id }">
                                <label class="custom-control-label" data-bind="text: Fornecedor.text, attr: { for: Fornecedor.id }"></label>
                            </div>
                        </div>
                    </div>
                    <div class="col-12 col-md-3 align-self-center" data-bind="visible: Terceiro.visible">
                        <div class="form-group">
                            <div class="custom-control custom-checkbox">
                                <input type="checkbox" class="custom-control-input" data-bind="checked: Terceiro.val, attr: { maxlength: Terceiro.maxlength, name: Terceiro.id, id: Terceiro.id }">
                                <label class="custom-control-label" data-bind="text: Terceiro.text, attr: { for: Terceiro.id }"></label>
                            </div>
                        </div>
                    </div>
                    <div class="col-12 col-md-3" data-bind="visible: Situacao.visible">
                        <div class="form-group">
                            <label class="form-label" data-bind="text: Situacao.text, attr: { for: Situacao.id }"></label>
                            <select class="form-control" data-bind="class: Situacao.requiredClass, options: Situacao.options, optionsText: 'text', optionsValue: 'value', value: Situacao.val, attr: { maxlength: Situacao.maxlength, id : Situacao.id}"></select>
                        </div>
                    </div>
                    <div class="col-12 col-md-12">
                        <div class="card" data-bind="visible: Campo.visible">
                            <div class="card-header d-flex align-items-center justify-content-between">
                                <h3 class="card-title">Campos Obrigatórios</h3>
                            </div>
                            <div>
                                <div class="card-body">
                                    <button class="btn btn-primary waves-effect waves-themed mb-2" type="button" data-bind="attr: { id: Campo.idBtnSearch, text: Campo.text }">
                                        <i class="fal fa-plus"></i><span data-bind="text: Campo.text"></span>
                                    </button>
                                    <table width="100%" class="table table-bordered table-hover" data-bind="attr: {id: GridCampo.id}" cellspacing="0"></table>
                                </div>

                            </div>

                        </div>
                    </div>
                </div>
                <footer id="knockoutCRUDPessoaCampoObrigatorio">
                    <div class="row mt-3">
                        <div class="col-6">
                            <button type="button" class="btn btn-labeled btn-default" data-bind="click: Cancelar.eventClick, visible: Cancelar.visible, attr: { id : Cancelar.id}">
                                <i class="fal fa-ban"></i>
                                <span data-bind="text: Cancelar.text"></span>
                            </button>
                        </div>
                        <div class="col-6 d-flex justify-content-end">
                            <button type="button" class="btn btn-labeled btn-danger mx-3" data-bind="click: Excluir.eventClick, visible: Excluir.visible, attr: { id : Excluir.id}">
                                <i class="fal fa-trash"></i>
                                <span data-bind="text: Excluir.text"></span>
                            </button>
                            <button type="button" class="btn btn-labeled btn-success" data-bind="click: Atualizar.eventClick, visible: Atualizar.visible, attr: { id : Atualizar.id}">
                                <i class="fal fa-check"></i>
                                <span data-bind="text: Atualizar.text"></span>
                            </button>
                            <button type="button" class="btn btn-labeled btn-success" data-loading-text="Aguarde..." data-bind="click: Adicionar.eventClick, visible: Adicionar.visible, attr: { id : Adicionar.id}">
                                <span data-bind="text: Adicionar.text"></span>
                            </button>
                        </div>
                    </div>
                </footer>
            </form>
        </div>
    </div>
</div>








<script src="~/scripts/pessoaCampoObrigatorio"></script>
<script type="text/javascript">

    pageSetUp();

    $(document).ready(function () {
        LoadPessoaCampoObrigatorio();
    });

</script>
