
<ol class="breadcrumb page-breadcrumb">
    <li class="breadcrumb-item">Cargas</li>
    <li class="breadcrumb-item active">Configuração Ocorrência Entrega</li>
</ol>

<div class="subheader">
    <h1 class="subheader-title">
        <i class=""></i> Configuração Ocorrência Entrega
    </h1>
</div>

<div id="knockoutPesquisaConfiguracaoOcorrenciaEntrega">
    <div class="panel">
        <div class="panel-container">
            <div class="panel-content">
                <button class="btn btn-primary waves-effect waves-themed" data-bind="click: ExibirFiltros.eventClick, id: ExibirFiltros.id">
                    <i class="fal fa-search"></i>
                    Pesquisar Configuração Entrega
                </button>

                <div class="mt-3" data-bind="id: ExibirFiltros.idFade, fadeVisible: ExibirFiltros.visibleFade">
                    <form>
                        <div class="row">
                            <div class="col-12 col-md-3" data-bind="visible: TipoAplicacaoColetaEntrega.visible">
                                <div class="form-group">
                                    <label class="form-label" data-bind="text: TipoAplicacaoColetaEntrega.text"></label>
                                    <select class="form-control" data-bind="options: TipoAplicacaoColetaEntrega.options, optionsText: 'text', optionsValue: 'value', value: TipoAplicacaoColetaEntrega.val, attr: { id : TipoAplicacaoColetaEntrega.id}"></select>
                                </div>
                            </div>
                            <div class="col-12 col-md-3" data-bind="visible: EventoColetaEntrega.visible">
                                <div class="form-group">
                                    <label class="form-label" data-bind="text: EventoColetaEntrega.text"></label>
                                    <select class="form-control" data-bind="options: EventoColetaEntrega.options, optionsText: 'text', optionsValue: 'value', value: EventoColetaEntrega.val, attr: { id : EventoColetaEntrega.id}"></select>
                                </div>
                            </div>
                            <div class="col-12 col-md-6" data-bind="visible: TipoOcorrencia.visible">
                                <div class="form-group">
                                    <label class="form-label" data-bind="text: TipoOcorrencia.text, attr: { for: TipoOcorrencia.id }"></label>
                                    <div class="input-group">
                                        <input type="text" data-bind="css: TipoOcorrencia.requiredClass, value: TipoOcorrencia.val, valueUpdate: 'afterkeydown', attr: { 'aria-label': TipoOcorrencia.text, maxlength: TipoOcorrencia.maxlength, id: TipoOcorrencia.id, name: TipoOcorrencia.id }" />
                                        <div class="input-group-append">
                                            <button class="btn btn-primary waves-effect waves-themed" type="button" data-bind="attr: { id: TipoOcorrencia.idBtnSearch}">
                                                <i class="fal fa-search"></i>
                                            </button>
                                        </div>
                                    </div>
                                </div>
                            </div>
                        </div>
                    </form>
                    <div class="d-flex align-items-center justify-content-end mb-3">
                        <button class="btn btn-primary waves-effect waves-themed btnPesquisarFiltroPesquisa" data-bind="click: Pesquisar.eventClick, attr: { id: Pesquisar.id}" type="button">
                            <i class="fal fa-search"></i><span data-bind="text: Pesquisar.text"></span>
                        </button>
                    </div>
                    <table width="100%" class="table table-bordered table-hover" data-bind="attr: { id: Pesquisar.idGrid}" cellspacing="0"></table>
                </div>
            </div>
        </div>
    </div>
</div>

<div class="panel">
    <div class="panel-container">
        <div class="panel-hdr">
            <h3>
                <i class="fal fa-edit"></i>
                Cadastrar Configuração de Ocorrência
            </h3>
        </div>
        <div class="panel-content">
            <form id="knockoutConfiguracaoOcorrenciaEntrega">
                <div class="row">
                    <div class="col-12 col-md-3" data-bind="visible: TipoAplicacaoColetaEntrega.visible">
                        <div class="form-group">
                            <label class="form-label" data-bind="text: TipoAplicacaoColetaEntrega.text"></label>
                            <select class="form-control" data-bind="options: TipoAplicacaoColetaEntrega.options, optionsText: 'text', optionsValue: 'value', value: TipoAplicacaoColetaEntrega.val, attr: { id : TipoAplicacaoColetaEntrega.id}"></select>
                        </div>
                    </div>
                    <div class="col-12 col-md-3" data-bind="visible: EventoColetaEntrega.visible">
                        <div class="form-group">
                            <label class="form-label" data-bind="text: EventoColetaEntrega.text"></label>
                            <select class="form-control" data-bind="options: EventoColetaEntrega.options, optionsText: 'text', optionsValue: 'value', value: EventoColetaEntrega.val, attr: { id : EventoColetaEntrega.id}"></select>
                        </div>
                    </div>
                    <div class="col-12 col-md-3" data-bind="visible: Reentrega.visible">
                        <div class="form-group">
                            <label class="form-label" data-bind="text: Reentrega.text"></label>
                            <select class="form-control" data-bind="options: Reentrega.options, optionsText: 'text', optionsValue: 'value', value: Reentrega.val, attr: { id : Reentrega.id}"></select>
                        </div>
                    </div>
                    <div class="col-12 col-md-3" data-bind="visible: AlvoDoPedido.visible">
                        <div class="form-group">
                            <label class="form-label" data-bind="text: AlvoDoPedido.text"></label>
                            <select class="form-control" data-bind="options: AlvoDoPedido.options, optionsText: 'text', optionsValue: 'value', value: AlvoDoPedido.val, attr: { id : AlvoDoPedido.id}"></select>
                        </div>
                    </div>
                    <div class="col-12 col-md-4" data-bind="visible: TipoOcorrencia.visible">
                        <div class="form-group">
                            <label class="form-label" data-bind="text: TipoOcorrencia.text, attr: { for: TipoOcorrencia.id }"></label>
                            <div class="input-group">
                                <input type="text" data-bind="css: TipoOcorrencia.requiredClass, value: TipoOcorrencia.val, valueUpdate: 'afterkeydown', attr: { 'aria-label': TipoOcorrencia.text, maxlength: TipoOcorrencia.maxlength, id: TipoOcorrencia.id, name: TipoOcorrencia.id }" />
                                <div class="input-group-append">
                                    <button class="btn btn-primary waves-effect waves-themed" type="button" data-bind="attr: { id: TipoOcorrencia.idBtnSearch}">
                                        <i class="fal fa-search"></i>
                                    </button>
                                </div>
                            </div>
                        </div>
                    </div>
                    <div class="col-12 col-md-2" data-bind="visible: TempoRecalculo.visible">
                        <div class="form-group">
                            <label class="form-label" data-bind="text: TempoRecalculo.text, attr: { for: TempoRecalculo.id }"></label>
                            <input type="text" class="form-control" data-bind="class: TempoRecalculo.requiredClass, value: TempoRecalculo.val, valueUpdate: 'afterkeydown', attr: { maxlength: TempoRecalculo.maxlength, id : TempoRecalculo.id}" />
                        </div>
                    </div>
                    <div class="col-12 col-md-4" data-bind="visible: TipoOperacao.visible">
                        <div class="form-group">
                            <label class="form-label" data-bind="text: TipoOperacao.text, attr: { for: TipoOperacao.id }"></label>
                            <div class="input-group">
                                <input type="text" data-bind="css: TipoOperacao.requiredClass, value: TipoOperacao.val, valueUpdate: 'afterkeydown', attr: { 'aria-label': TipoOperacao.text, maxlength: TipoOperacao.maxlength, id: TipoOperacao.id, name: TipoOperacao.id }" />
                                <div class="input-group-append">
                                    <button class="btn btn-primary waves-effect waves-themed" type="button" data-bind="attr: { id: TipoOperacao.idBtnSearch}">
                                        <i class="fal fa-search"></i>
                                    </button>
                                </div>
                            </div>
                        </div>
                    </div>
                </div>
                <footer>
                    <div class="row">
                        <div class="col-6">
                            <button type="button" class="btn btn-labeled btn-default" data-bind="click: Cancelar.eventClick, visible: Cancelar.visible, attr: { value: Cancelar.text, id : Cancelar.id}">
                                <i class="fal fa-ban"></i>
                                <span data-bind="text: Cancelar.text"></span>
                            </button>
                        </div>
                        <div class="col-6 d-flex justify-content-end">
                            <button type="button" class="btn btn-labeled btn-danger me-2" data-bind="click: Excluir.eventClick, visible: Excluir.visible, attr: { value: Excluir.text, id : Excluir.id}">
                                <i class="fal fa-trash"></i>
                                <span data-bind="text: Excluir.text"></span>
                            </button>
                            <button type="button" class="btn btn-labeled btn-success" data-bind="click: Atualizar.eventClick, visible: Atualizar.visible, attr: { value: Atualizar.text, id : Atualizar.id}">
                                <i class="fal fa-check"></i>
                                <span data-bind="text: Atualizar.text"></span>
                            </button>
                            <button type="button" class="btn btn-labeled btn-success" data-loading-text="Aguarde..." data-bind="click: Adicionar.eventClick, visible: Adicionar.visible, attr: { value: Adicionar.text, id : Adicionar.id}">
                                <span data-bind="text: Adicionar.text"></span>
                            </button>
                        </div>
                    </div>
                </footer>
            </form>
        </div>
    </div>
</div>

<script src="~/scripts/configuracaoOcorrenciaEntrega"></script>

<script type="text/javascript">

    pageSetUp();

    $(document).ready(function () {
        loadConfiguracaoOcorrenciaEntrega();
    });

</script>

